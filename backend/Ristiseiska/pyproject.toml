[project]
name = "ristiseiska"
version = "0.1.0"
description = "Ristiseiska game engine"
authors = [{name = "Ari"}]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"